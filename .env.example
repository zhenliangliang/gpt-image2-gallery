# Optional: use X official API search instead of public mirror parsing.
X_BEARER_TOKEN=

# Optional: public Nitter-compatible mirror used when X_BEARER_TOKEN is empty.
NITTER_BASE_URL=https://nitter.net

# Optional: override the default search query.
DEFAULT_SEARCH_QUERY="gpt-image2 OR gpt-image-2 OR \"gpt image 2\""
